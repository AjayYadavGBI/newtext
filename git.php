// there are some git commands which need to be revised everyday......

git init
git status
git pull origin master
git push origin master
git branch
git checkout branch_name
git branch new_branch
// merge branch

git branch --merged // locally
git merge my_branch
git push origin master


//Deete Branch

git branch --merged
git branch -d my_branch //Locally
git push origin --delete my_branch // Remote location

echo "content inside the new file" > fileName.txt

